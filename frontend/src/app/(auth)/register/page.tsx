export default function Page() {
  // Signup form: email, password, name, base currency (from /currencies), locale, timezone (Phase 2).
  return (
    <main className="p-6">
      <h1 className="text-2xl font-semibold">Create account</h1>
      <p className="text-muted-foreground">Scaffolded route — build out per docs/claude-code-build-plan.md.</p>
    </main>
  );
}
