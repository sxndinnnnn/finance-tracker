export default function Page() {
  // Card list; each card shows its linked subscriptions (Phase 4).
  return (
    <main className="p-6">
      <h1 className="text-2xl font-semibold">Credit Cards</h1>
      <p className="text-muted-foreground">Scaffolded route — build out per docs/claude-code-build-plan.md.</p>
    </main>
  );
}
