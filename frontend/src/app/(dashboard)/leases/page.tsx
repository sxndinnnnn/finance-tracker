export default function Page() {
  // Lease list with payment schedule (Phase 4).
  return (
    <main className="p-6">
      <h1 className="text-2xl font-semibold">Leases</h1>
      <p className="text-muted-foreground">Scaffolded route — build out per docs/claude-code-build-plan.md.</p>
    </main>
  );
}
