export default function Page() {
  // Subscription list, linked card, next billing date, reminder lead time (Phase 5).
  return (
    <main className="p-6">
      <h1 className="text-2xl font-semibold">Subscriptions</h1>
      <p className="text-muted-foreground">Scaffolded route — build out per docs/claude-code-build-plan.md.</p>
    </main>
  );
}
